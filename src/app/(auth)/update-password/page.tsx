import { UpdatePassword } from "@/components/AuthComponents";

export default function page(){
    return(
        <div className="w-full h-screen flex items-center justify-center bg-slate-800">
            <UpdatePassword/>
        </div>
    )
}